<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FontSize"
             Value="20"/>
            <Setter Property="FontFamily"
              Value="Ariel"/>
            <Setter Property="FontWeight"
           Value="Bold"/>
            <!--Trigger for mouse enter -->
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX="1.1" ScaleY="1.1"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>
    <Grid Margin="0,12,0,-12">
        <Grid.Background>
            <ImageBrush ImageSource="/mainwindowbackround.jpg" Opacity="0.7"/>
        </Grid.Background>
        <!--A Botton for all agent,s content-->
        <Button Content="Handle Agents" HorizontalAlignment="Left" Margin="170,90,0,0" VerticalAlignment="Top" Height="100" Width="200" Click="btnAgents_Click" FontWeight="Bold" FontSize="25" FontStyle="Italic"/>
        <!--A Botton for all task's content-->
        <Button Content="Handle Tasks" HorizontalAlignment="Left" Margin="170,210,0,0" VerticalAlignment="Top" Height="100" Width="200"  RenderTransformOrigin="-0.523,0.537" Click="btnTasks_Click" FontStyle="Italic" FontSize="25"/>
        <!--A button for Initialization-->
        <Button Content="Initialize Data" HorizontalAlignment="Left" Margin="450,90,0,0" VerticalAlignment="Top" Height="100" Width="200" Click="btnInitialize_Click" FontSize="25" FontStyle="Italic"/>
        <!--Reset button-->
        <Button Content="Reset Data" HorizontalAlignment="Left" Margin="450,210,0,0" VerticalAlignment="Top" Height="100" Width="200" Click="btnReset_Click" FontSize="25" FontStyle="Italic"/>
    </Grid>
</Window>
